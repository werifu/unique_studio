class Table:
    def __init__(self, id, col1, col2):
        self.id = id
        self.col1 = col1
        self.col2 = col2
object1 = Table(id1, value1, value2)


use DB  #使用DB数据库
DB.CreateTable(Table, object1)
DB.Create(Table, object1)
DB.Update(Table, object1)
DB.Delete(Table, object2)
DB.FindOne(Table, object.col, "condition")
DB.FindAll(Table, object.col, int max_return_num, tuple tuple_name)
